# JobSpy (https://github.com/speedyapply/JobSpy) — cubre Indeed Ecuador y
# LinkedIn. Las otras tres fuentes (Bolsa EPN, Multitrabajos, Computrabajo)
# van con scrapers propios sobre Playwright, porque ninguna API las cubre y
# son justo las que publican pasantias para estudiantes de la EPN.
python-jobspy==1.1.82

# Playwright mueve los scrapers propios. Necesita un Chromium: en el VPS
# (Linux ARM64) Playwright NO publica binario, asi que la imagen instala el
# Chromium del sistema y se apunta ahi con SCRAPER_CHROMIUM (ver Dockerfile
# y panchito/sources/base.py::lanzar_navegador).
playwright==1.49.1

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Versiones fijadas a proposito: un scraper que se auto-actualiza es un
# scraper que un dia deja de arrancar en produccion sin que nadie tocara
# nada. Subirlas es una decision explicita, no un efecto de reconstruir la
# imagen.
