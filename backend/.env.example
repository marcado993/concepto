# Copiar a .env — nunca commitear el .env real (ya cubierto por
# policy/semgrep-rules.yml + Gitleaks en el pipeline).

DATABASE_URL="postgresql://user:password@localhost:5432/aeis_app?schema=public"

# Logto self-hosted (RESUELTO — docs/dominio/02-necesidades-stakeholders.md
# §4 #5), corriendo en la misma instancia OCI vía docker-compose.prod.yml.
# LOGTO_ISSUER apunta al hostname "auth." (puerto 3001, el que ve el
# estudiante) — NO al "admin." (puerto 3002, la consola donde se configura
# la app/conectores), ese nunca lo toca el backend.
LOGTO_ISSUER="https://auth.aeis-app.online/oidc"
LOGTO_AUDIENCE="https://api.aeis-app.online"

# App de tipo "Traditional Web" en Logto (no SPA) — el backend hace el
# intercambio de código, por eso necesita client_secret. Requiere tener
# habilitado el conector social de GitHub en el tenant de Logto.
LOGTO_APP_ID="<app-id-de-logto>"
LOGTO_APP_SECRET="<app-secret-de-logto>"
LOGTO_REDIRECT_URI="https://api.aeis-app.online/auth/callback"

# Login social embebido (GitHub/Google) vía la Experience API de Logto —
# ver logto-experience.client.ts. El ID de cada conector es INTERNO de
# Logto (no "github"/"google"), se lee del Callback URI que Logto muestra
# en el detalle del conector (Console → Connectors → [conector] → Callback
# URI: .../callback/{ese-id}) o de GET /api/.well-known/sign-in-exp
# (campo socialConnectors[].id).
LOGTO_GITHUB_CONNECTOR_ID="<id-interno-del-conector-github-en-logto>"
LOGTO_GOOGLE_CONNECTOR_ID="<id-interno-del-conector-google-en-logto>"

# A dónde redirige GitHub/Google tras el login (nunca a Logto) — este
# backend, NO el frontend. Tiene que estar agregado como callback URL
# autorizado en el OAuth App de GitHub y en el cliente OAuth de Google
# Cloud, ADEMÁS del callback propio de Logto que ya tenían — sin eso el
# proveedor rechaza el intercambio con redirect_uri_mismatch.
SOCIAL_REDIRECT_URI="https://api.aeis-app.online/auth/social/callback"

# Firma la cookie de PKCE (backend/src/shared/auth/auth.controller.ts) —
# generar con `openssl rand -base64 32`, nunca reutilizar entre entornos.
COOKIE_SECRET="<valor-aleatorio-largo>"

# Firma los JWT del panel de administración (backend/src/admin/admin-auth/)
# — login propio de correo+contraseña, completamente aparte de Logto/User.
# Generar con `openssl rand -base64 48`, nunca reutilizar entre entornos ni
# con COOKIE_SECRET/otros secretos.
ADMIN_JWT_SECRET="<valor-aleatorio-largo-y-distinto>"

# Cajita de Pagos de PayPhone (payphonetodoesposible.com) — se obtienen en
# Payphone Developer creando una app tipo "WEB" con el dominio real de
# producción y una URL de respuesta que apunte a la raíz del frontend
# (PayPhone redirige ahí con ?id=&clientTransactionId= tras el pago). Sin
# estos dos valores, GET /lockers/payphone/config responde
# configured:false y el frontend deshabilita esa opción de pago.
PAYPHONE_TOKEN="<token-de-payphone-developer>"
PAYPHONE_STORE_ID="<storeid-de-payphone-developer>"

# Correo TRANSACCIONAL propio del backend (contratos de casillero — ver
# locker-contract.ts) — proveedor: Resend (resend.com), dominio propio
# verificado. Distinto del correo de login por OTP, que vive DENTRO de
# Logto y usa su propia cuenta de Resend, no esta.
RESEND_API_KEY="<api-key-de-resend>"
MAIL_FROM="contratos@mail.aeis.app"

# Alertas de CPU/memoria (shared/monitoring) — una o más URLs separadas por
# coma, cada una recibe un POST JSON {text, severity, source, at}.
# Recomendado para celular: crea un topic privado en https://ntfy.sh (sin
# registro) y suscríbete desde la app de ntfy — la URL es
# https://ntfy.sh/<tu-topic-secreto>. Para correo, cualquier automatización
# que reciba ese mismo POST y reenvíe por email sirve igual.
#
# IMPORTANTE: reemplaza <tu-topic-secreto> por un nombre LARGO Y ALEATORIO
# (los topics de ntfy son públicos: quien adivine el nombre lee y publica en
# él). Si dejas el placeholder tal cual, el backend lo detecta y NO envía
# nada (ver el guard isRealWebhook en alert.service.ts) — pero entonces te
# quedas sin alertas. Un topic real se ve como https://ntfy.sh/aeis-a8f3d9c1e7.
ALERT_WEBHOOK_URLS="https://ntfy.sh/<tu-topic-secreto>"

# Origen(es) del frontend, separados por coma — nunca "*" (el backend maneja
# pagos y datos personales). Incluye los dominios reales de producción
# (https://aeis.app, https://www.aeis.app, el dominio de Vercel del proyecto)
# y los http://localhost:* del harness e2e (Playwright corre el frontend en
# localhost:4173/5173 contra ESTA API real para las pruebas de integración —
# sin estos orígenes, CORS bloquea esas pruebas). Se evaluó quitar los
# localhost en producción (auditoría de seguridad): el riesgo real es casi
# nulo — una página servida desde el localhost de la víctima no puede leer el
# JWT que vive en el localStorage de aeis.app (mismo-origen), así que no gana
# nada con estar en la allowlist — y quitarlos rompía el harness e2e. Se
# quedan a propósito.
#
# panel.aeis-app.online al final (ver Caddyfile): el panel de administración
# (precios/usuarios/auditoría) es un build APARTE (vite.admin.config.ts),
# servido solo en la red Tailscale, nunca por Vercel — pero igual habla con
# ESTE backend (api.aeis-app.online) desde su propio origen, así que necesita
# estar en la allowlist de CORS como cualquier otro. Va al final a propósito:
# el PRIMER origen de esta lista es al que Logto redirige tras un login
# social (ver auth.controller.ts/*.strategy.ts) — debe seguir siendo
# https://aeis.app, no este.
FRONTEND_ORIGIN="https://aeis.app,https://www.aeis.app,https://concepto-7tts.vercel.app,http://localhost:5173,http://localhost:4173,https://panel.aeis-app.online"

PORT=3000

# Poner en "true" SOLO si el backend corre detrás de un reverse proxy
# conocido (Nginx/Caddy en el mismo VPS) — de lo contrario, X-Forwarded-For
# se puede falsificar y rompe tanto el rate limiting por IP como el
# ipAddress del AuditLog.
TRUST_PROXY=false
